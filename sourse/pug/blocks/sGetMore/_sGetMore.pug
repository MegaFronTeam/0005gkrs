mixin sGetMore(data)
	// start sGetMore
	+b.SECTION.sGetMore.section#sGetMore&attributes(attributes)
		.container
			+b.section-title.text-center
				h2 С&nbsp;этим товаром часто покупают
			
			.row
	// end sGetMore